# CHECKPOINT — Global Helper Shell + Current Routes Registry Pass V7.12.128

Status: FULL GLOBAL HELPER / REGISTRY PASS

Date: 2026-05-29

## User-tested pass

Passed URL:

- `https://chatterfriendsstreambandit.co.uk/stream-bandit-global-helper-shell-v7-12-126-test.html`

User confirmed:

- Header still looks like Stream Bandit.
- Account button opens.
- Avatar appears when signed in and profile has avatar_url.
- Overlay menu opens.
- Movie search overlay works from Supabase `sb_movies`.
- Extra owner icons are useful and duplicates are removed.
- Builder opens V7.12.116.
- Preview opens V7.12.117.
- Admin opens V7.12.121.
- Registry route works.
- Policy opens V7.12.119/120.
- Footer appears once.
- No payment buttons appear.

## Important route note

Old stored Registry route still exists in protected shell:

- `all-pages-version-registry-v7-1-4-full-test.html`

Passed promoted route:

- `all-pages-version-registry-v7-12-122-current-routes-test.html`

The global helper loader stabilises/promotes this at runtime until protected shell route URLs are deliberately cleaned.

## Global Whatnots swap

Old page:

- `global-helper-property-owner-scan-v7-12-67-test.html`

Old status:

- Parked / disabled because the scanner caused browser instability.

New status:

- Replaced with Global Helper Shell + Current Routes Registry pass page.

## Global helper loader

Passed loader file:

- `stream-bandit-global-helper-loader-v7-12-126.js`

Purpose:

- Keep existing helper shells.
- Stabilise movie overlay search from `sb_movies`.
- Promote mismatched route URLs.
- Keep one clean owner icon row.
- Preserve account/auth/avatar/theme/footer/favicon/settings helpers.
- No Supabase writes.
- No payment system.
- No index promotion action.

## Index promotion

`index.html` promoted to:

- `Stream Bandit V7.12.128 Global Helper + Registry Pass`

Index remains a checkpoint/redirect page that opens Home.

## Domain and contact

Live domain:

- `https://chatterfriendsstreambandit.co.uk`

Website email:

- `info@chatterfriendsstreambandit.co.uk`

## No payment system

Payments, checkout, Stripe, subscriptions and premium billing remain off until intentionally built.

## Next workflow

Continue from Settings group and Policy group because the helper/route stabiliser was needed there.

Then resume normal group workflow:

1. Settings / Policy confirmation.
2. Admin/User Management route checks.
3. Owner group checks.
4. Then continue next menu group page-by-page.

## Rule kept

Existing helper shells were preserved. The project now uses the passed helper loader as the global stabiliser instead of replacing the protected shell directly.
