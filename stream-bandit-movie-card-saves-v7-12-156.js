/* Stream Bandit Movie Card Saves V7.12.156 — ROLLED BACK SAFE STUB
   Disabled after refresh/loop issue. Kept as a harmless file because some pages/bridges may request it.
   Does not inject buttons, observe DOM, refresh shells, or write anything. */
(function(){
'use strict';
const VERSION='V7.12.156 Movie Card Saves — disabled rollback stub';
function state(){return{version:VERSION,disabled:true,reason:'Rolled back after refresh/loop issue. Details keeps its own save buttons.'};}
function refresh(){return state();}
function inject(){return 0;}
function refreshButtons(){return state();}
window.StreamBanditMovieCardSaves={version:VERSION,disabled:true,refresh,inject,refreshButtons,state};
document.documentElement.dataset.sbMovieCardSaves='disabled-rollback';
})();
