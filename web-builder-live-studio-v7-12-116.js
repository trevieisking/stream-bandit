/* Stream Bandit V7.12.116.1 Web Builder Studio overlay publish owner-scope repair
   Keeps the passed V7.12.106 overlay Builder. Runtime-patches only:
   - same-window links
   - owned Web Builder route links
   - page selector scoped by owner_id for non-platform-owner users
   - load/save scoped by owner_id for non-platform-owner users
   - no admin role bypass for personal Web Builder rows
   - long-session save refresh/re-read/verify
   - publish notice panel with exact status/error
*/
(function(){
'use strict';
const PATCH_VERSION='V7.12.116.1 Web Builder Studio publish owner-scope repair';
function setStatus(msg,bad){try{let el=document.getElementById('status');if(el){el.textContent=msg;el.className=bad?'note':'status';}}catch(e){}}
function patchSource(src){
  src=String(src||'');
  src=src.replace("const VERSION='V7.12.106 Web Builder Studio';","const VERSION='"+PATCH_VERSION+"';");
  src=src.replaceAll('target="_blank"','');
  src=src.replaceAll('<span class="pill">V7.12.106</span>','<span class="pill">V7.12.116.1</span>');
  src=src.replaceAll("document.documentElement.dataset.webBuilderStudio='v7-12-106';","document.documentElement.dataset.webBuilderStudio='v7-12-116-1';");

  src=src.replace("let sb=null,user=null,blocks=[],selected=-1,editing=-1,draft=null,hlsLoading=false;","let sb=null,user=null,wbProfile=null,wbIsPlatformOwner=false,wbCanUseBuilder=false,blocks=[],selected=-1,editing=-1,draft=null,hlsLoading=false;");

  src=src.replace("function prevUrl(){return 'web-builder-shared-style-preview-v7-9-2-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}","function prevUrl(){return 'web-builder-preview-owned-v7-12-257-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}");
  src=src.replace("function formUrl(){return 'web-builder-form-save-v7-12-94-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}","function formUrl(){return 'web-builder-form-designer-owned-v7-12-258-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}");
  src=src.replace("function inboxUrl(){return 'web-builder-form-submissions-v7-12-94-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}","function inboxUrl(){return 'web-builder-form-inbox-owned-v7-12-258-test.html?page='+encodeURIComponent(slug($('wbSlug').value));}");

  const ownerHelpers="function wbObj(v){if(v&&typeof v==='object')return v;if(typeof v==='string'&&v.trim()){try{return JSON.parse(v)||{};}catch(e){}}return{};}function wbTruth(v){if(v===true||v===1)return true;if(typeof v==='string')return ['true','1','yes','on','enabled','allow','allowed'].includes(v.toLowerCase());return false;}function wbRank(k){return ({free_viewer:0,viewer_plus:1,creator_starter:2,creator_growth:3,creator_pro:4,studio_business:5,platform_owner:6,enterprise:7})[String(k||'free_viewer')]||0;}function wbOwnerProfile(p){return !!p&&(p.admin_level==='owner'||p.plan_key==='platform_owner');}function wbBuilderAllowed(p){if(!p)return false;if(wbOwnerProfile(p))return true;if(String(p.account_status||'active').toLowerCase()!=='active')return false;let ps=wbObj(p.permissions_json);return wbRank(p.plan_key)>=wbRank('creator_growth')||wbTruth(ps.web_builder)||wbTruth(ps.web_builder_starter)||wbTruth(ps.pages_manager)||wbTruth(ps.web_builder_studio);}async function wbRefreshAuthority(){if(!sb)return;user=null;wbProfile=null;wbIsPlatformOwner=false;wbCanUseBuilder=false;try{let u=await sb.auth.getUser();user=u.data&&u.data.user||null;}catch(e){}if(user){try{let pr=await sb.from('sb_profiles').select('id,role,admin_level,plan_key,permissions_json,account_status').eq('id',user.id).maybeSingle();wbProfile=pr.data||null;wbIsPlatformOwner=wbOwnerProfile(wbProfile);wbCanUseBuilder=wbBuilderAllowed(wbProfile);}catch(e){wbProfile=null;wbIsPlatformOwner=false;wbCanUseBuilder=false;}}}\n";
  const oldClient="async function client(){if(sb)return sb;let t=await fetch('stream-bandit-shell-v6-24.js?v=7-12-104',{cache:'no-store'}).then(r=>r.text());let url=(t.match(/SUPABASE_URL\\s*=\\s*'([^']+)'/)||[])[1];let key=(t.match(/SUPABASE_KEY\\s*=\\s*'([^']+)'/)||[])[1];sb=supabase.createClient(url,key);try{let u=await sb.auth.getUser();user=u.data&&u.data.user||null;}catch(e){}return sb;}";
  const newClient=ownerHelpers+"async function client(){if(sb){await wbRefreshAuthority();return sb;}let t=await fetch('stream-bandit-shell-v6-24.js?v=7-12-104',{cache:'no-store'}).then(r=>r.text());let url=(t.match(/SUPABASE_URL\\s*=\\s*'([^']+)'/)||[])[1];let key=(t.match(/SUPABASE_KEY\\s*=\\s*'([^']+)'/)||[])[1];sb=supabase.createClient(url,key);await wbRefreshAuthority();return sb;}";
  if(src.includes(oldClient))src=src.replace(oldClient,newClient);else setStatus('V7.12.116.1 repair warning: client function not found for owner-scope patch.',true);

  const noticeHelpers="function publishNotice(msg,bad){let box=$('wbPublishNotice');if(!box)return status(msg,bad);let line=document.createElement('div');line.textContent=new Date().toLocaleTimeString()+' — '+msg;line.style.margin='4px 0';line.style.color=bad?'#ffd8df':'#dfffee';box.prepend(line);status(msg,bad);}function setSaveBusy(on){let b=$('wbSave');if(!b)return;b.disabled=!!on;b.style.opacity=on?'.65':'1';b.textContent=on?'Saving...':'Save + Publish';}\n";
  const oldLinks="function updateLinks(){if(!$('wbOpenPreview'))return;$('wbOpenPreview').href=prevUrl();$('wbOpenForm').href=formUrl();$('wbOpenInbox').href=inboxUrl();}";
  const newLinks=noticeHelpers+"function updateLinks(){if(!$('wbOpenPreview'))return;$('wbOpenPreview').href=prevUrl();$('wbOpenForm').href=formUrl();$('wbOpenInbox').href=inboxUrl();let ps=$('wbPageSelect');if(ps){let cur=slug($('wbSlug').value);[...ps.options].forEach(o=>{if(o.value===cur)ps.value=cur;});}}\nasync function loadPageChoices(){try{await client();let sel=$('wbPageSelect');if(!sel)return;if(!user){sel.innerHTML='<option value=\"\">Sign in to load your pages...</option>';return;}if(!wbCanUseBuilder){sel.innerHTML='<option value=\"\">Web Builder access is not enabled...</option>';return;}let q=sb.from('sb_site_pages').select('slug,title,status,updated_at,owner_id').order('updated_at',{ascending:false});if(!wbIsPlatformOwner)q=q.eq('owner_id',user.id);q=q.limit(150);let r=await q;if(r.error)throw r.error;let cur=slug($('wbSlug').value);sel.innerHTML='<option value=\"\">Choose a saved page / slug...</option>'+((r.data||[]).map(p=>'<option value=\"'+esc(p.slug)+'\" '+(p.slug===cur?'selected':'')+'>'+esc((p.title||p.slug)+' — '+p.slug+' ['+(p.status||'draft')+']'+(wbIsPlatformOwner&&p.owner_id?' owner '+String(p.owner_id).slice(0,8):''))+'</option>').join(''));}catch(e){publishNotice('Page selector failed: '+(e.message||e),true);}}";
  src=src.replace(oldLinks,newLinks);

  src=src.replace("<div class=\"wbToolbar\"><button id=\"wbAddBlock\" class=\"wbBtn\">+ Add Block</button>","<div id=\"wbPublishNotice\" class=\"note\" style=\"width:100%;max-height:150px;overflow:auto\"><b>Publish notices</b><div>Waiting for Save + Publish...</div></div><div class=\"wbToolbar\"><select id=\"wbPageSelect\" class=\"wbSmall\" style=\"min-width:260px;background:#0006;color:white;border:1px solid #ffffff24;border-radius:999px;padding:10px 13px\"><option value=\"\">Choose a saved page / slug...</option></select><button id=\"wbAddBlock\" class=\"wbBtn\">+ Add Block</button>");

  const oldLoad="async function load(){try{await client();let s=slug($('wbSlug').value);$('wbSlug').value=s;let r=await sb.from('sb_site_pages').select('*').eq('slug',s).maybeSingle();if(r.error)throw r.error;if(!r.data){blocks=[];selected=-1;render();status('No saved page found. Use Starter Pack.');return;}$('wbTitle').value=r.data.title||s;$('wbStatusSelect').value=r.data.status||'published';blocks=Array.isArray(r.data.layout_json)?r.data.layout_json.map(safe):[];selected=blocks.length?0:-1;render();status('Loaded '+s+'.');}catch(e){status('Load failed: '+(e.message||e),true);}}";
  const newLoad="async function load(){try{await client();let s=slug($('wbSlug').value);$('wbSlug').value=s;if(!user)throw new Error('Sign in required for Web Builder Studio.');if(!wbCanUseBuilder)throw new Error('This account does not have Web Builder access.');let q=sb.from('sb_site_pages').select('*').eq('slug',s);if(!wbIsPlatformOwner)q=q.eq('owner_id',user.id);let r=await q.maybeSingle();if(r.error)throw r.error;if(!r.data){blocks=[];selected=-1;render();status('No saved page found in this personal workspace. Use Starter Pack.');return;}$('wbTitle').value=r.data.title||s;$('wbStatusSelect').value=r.data.status||'published';blocks=Array.isArray(r.data.layout_json)?r.data.layout_json.map(safe):[];selected=blocks.length?0:-1;render();status('Loaded '+s+' from '+(wbIsPlatformOwner?'platform-owner all-row scope.':'personal owner_id scope.'));}catch(e){status('Load failed: '+(e.message||e),true);}}";
  if(src.includes(oldLoad))src=src.replace(oldLoad,newLoad);else setStatus('V7.12.116.1 repair warning: exact old load function not found.',true);

  const oldSave="async function save(){try{await client();let s=slug($('wbSlug').value);let row={slug:s,title:$('wbTitle').value||s,page_type:'custom',status:$('wbStatusSelect').value||'published',layout_json:blocks.map(safe),settings_json:{builder:'web-builder-live-studio-v7-12-106.js',preview:prevUrl(),advancedForm:formUrl(),formInbox:inboxUrl(),protectedShell:'stream-bandit-shell-v6-24.js'},updated_at:new Date().toISOString()};if(user)row.owner_id=user.id;let r=await sb.from('sb_site_pages').upsert(row,{onConflict:'slug'}).select('*').maybeSingle();if(r.error)throw r.error;status('Saved. Published Preview, Form and Inbox routes are ready.');debug();}catch(e){status('Save failed: '+(e.message||e),true);}}";
  const newSave="async function save(){setSaveBusy(true);try{publishNotice('Save clicked. Refreshing Supabase client/session...');sb=null;await client();if(!user)throw new Error('Sign in required for Web Builder Studio.');if(!wbCanUseBuilder)throw new Error('This account does not have Web Builder save access.');let s=slug($('wbSlug').value);$('wbSlug').value=s;publishNotice('Re-reading existing page row for '+s+' using '+(wbIsPlatformOwner?'platform-owner all-row scope.':'personal owner_id scope.') );let existingQ=sb.from('sb_site_pages').select('*').eq('slug',s);if(!wbIsPlatformOwner)existingQ=existingQ.eq('owner_id',user.id);existingQ=existingQ.limit(1);let existingRes=await existingQ;if(existingRes.error)throw existingRes.error;let existing=existingRes.data&&existingRes.data[0]||null;let existingSettings=existing&&existing.settings_json&&typeof existing.settings_json==='object'?existing.settings_json:{};let settings=Object.assign({},existingSettings);settings.builder='web-builder-live-studio-v7-12-116.js';settings.preview=prevUrl();settings.advancedForm=formUrl();settings.formInbox=inboxUrl();settings.protectedShell='web-builder-protected-page-v7-12-265.js';settings.lastBuilderPublishAt=new Date().toISOString();settings.publishRepair='V7.12.116.1';settings.personal_workspace_scope=wbIsPlatformOwner?'platform_owner_all_rows':'owner_id_only';let outStatus=$('wbStatusSelect').value||'published';if(outStatus!=='hidden')outStatus='published';let row={slug:s,title:$('wbTitle').value||s,page_type:existing&&existing.page_type||'custom',status:outStatus,layout_json:blocks.map(safe),settings_json:settings,updated_at:new Date().toISOString(),owner_id:(wbIsPlatformOwner&&existing&&existing.owner_id)?existing.owner_id:user.id};let saved=null;if(existing){publishNotice('Saving existing scoped page row with '+row.layout_json.length+' blocks...');let upQ=sb.from('sb_site_pages').update(row).eq('id',existing.id);if(!wbIsPlatformOwner)upQ=upQ.eq('owner_id',user.id);let up=await upQ.select('*').maybeSingle();if(up.error)throw up.error;saved=up.data||existing;}else{publishNotice('Creating new personal page row with '+row.layout_json.length+' blocks...');let ins=await sb.from('sb_site_pages').insert(row).select('*').maybeSingle();if(ins.error)throw ins.error;saved=ins.data;}publishNotice('Supabase save returned OK. Verifying saved row in same scope...');let verifyQ=sb.from('sb_site_pages').select('slug,status,layout_json,settings_json,updated_at,owner_id').eq('id',saved.id);if(!wbIsPlatformOwner)verifyQ=verifyQ.eq('owner_id',user.id);let verify=await verifyQ.maybeSingle();if(verify.error)throw verify.error;if(!verify.data)throw new Error('Save verification failed: row not visible inside this workspace.');let count=Array.isArray(verify.data&&verify.data.layout_json)?verify.data.layout_json.length:0;if(count!==row.layout_json.length)throw new Error('Verify mismatch: tried to save '+row.layout_json.length+' blocks but read back '+count);let navOk=!!(verify.data&&verify.data.settings_json&&verify.data.settings_json.navigation);publishNotice('Saved + published. Verified '+count+' blocks persisted. Navigation '+(navOk?'preserved.':'not present on this row.')+' Owner '+String(verify.data.owner_id||'').slice(0,8)+'.');setTimeout(loadPageChoices,400);debug();}catch(e){publishNotice('Save failed: '+(e.message||e),true);let d=$('debug');if(d)d.textContent=JSON.stringify({saveError:e.message||String(e),stack:e.stack||null},null,2);}finally{setSaveBusy(false);}}";
  if(!src.includes(oldSave)){
    setStatus('V7.12.116.1 repair warning: exact old save function not found; using fallback patch.',true);
    src=src.replace(/async function save\(\)\{try\{[\s\S]*?debug\(\);\}catch\(e\)\{status\('Save failed: '\+\(e\.message\|\|e\),true\);\}\}/,newSave);
  }else src=src.replace(oldSave,newSave);

  src=src.replace("function wire(){Object.keys(TYPES).forEach(k=>","function wire(){setTimeout(loadPageChoices,900);document.addEventListener('change',e=>{if(e.target&&e.target.id==='wbPageSelect'&&e.target.value){$('wbSlug').value=e.target.value;load();}});Object.keys(TYPES).forEach(k=>");
  src=src.replace("$('wbLoad').onclick=load;$('wbSave').onclick=save;","$('wbLoad').onclick=()=>{load();setTimeout(loadPageChoices,500);};$('wbSave').onclick=()=>{publishNotice('Save + Publish button clicked.');save();};");
  return src;
}
async function boot(){
  try{
    setStatus('Loading V7.12.116.1 overlay publish owner-scope repair engine...');
    const res=await fetch('web-builder-live-studio-v7-12-106.js?v=7-12-116-1-source',{cache:'no-store'});
    if(!res.ok)throw new Error('Could not load V7.12.106 source: '+res.status);
    const original=await res.text();
    const patched=patchSource(original)+"\n//# sourceURL=web-builder-live-studio-v7-12-116-runtime.js";
    const script=document.createElement('script');
    script.text=patched;
    document.head.appendChild(script);
  }catch(e){
    console.error(e);
    setStatus('V7.12.116.1 builder repair failed to load: '+(e.message||e),true);
    const d=document.getElementById('debug');
    if(d)d.textContent=String(e&&e.stack||e);
  }
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
