/* Stream Bandit Social Actions V7.13.004
   Visible Share/Edit/Remove repair for social posts plus Friends profile-card sharing.
   Post edit/delete uses existing SECURITY DEFINER RPC helpers:
   - sb_social_update_own_post
   - sb_social_remove_own_post
   The RPCs still guard author_id = auth.uid(). No schema, RLS or storage policy changes.
*/
(function(){
'use strict';

const VERSION='V7.13.004 Social Actions + Friends Profile Share';
const SOCIAL_FILES=new Set(['news-feed-social-v7-13-001-test.html','profile-social-v7-13-001-test.html','groups-social-v7-13-001-test.html','friends-social-v7-13-001-test.html']);
const SHARE_HELPER='stream-bandit-social-share-v7-13-001.js?v=social-actions-7-13-004';
let sb=null,user=null,cache=new Map(),running=false;

function file(){return String(location.pathname||'').split('/').pop()||'index.html';}
function isSocial(){return SOCIAL_FILES.has(file())||!!(document.body&&document.body.dataset&&Object.keys(document.body.dataset).some(k=>/^sbSocial/.test(k)));}
function clean(v,n){v=String(v||'').replace(/\s+/g,' ').trim();return n&&v.length>n?v.slice(0,n-1).trim()+'...':v;}
function toast(msg){let t=document.createElement('div');t.className='toast';t.textContent=msg;if(!document.querySelector('.toast'))t.style.cssText='position:fixed;right:18px;bottom:18px;background:#12172a;color:#fff;border:1px solid #ffffff35;border-radius:16px;padding:13px 16px;box-shadow:0 16px 44px #0009;font-weight:900;z-index:100001';document.body.appendChild(t);setTimeout(()=>t.remove(),3200);}
function loadScript(src){try{let base=src.split('?')[0];if(Array.from(document.scripts||[]).some(s=>String(s.src||'').includes(base)))return;let s=document.createElement('script');s.src=src;s.defer=true;s.dataset.sbLoadedBy='social-actions-v7-13-004';document.head.appendChild(s);}catch(e){}}
function cfg(){try{let c=window.StreamBanditShell&&window.StreamBanditShell.config&&window.StreamBanditShell.config();if(c&&c.url&&c.key)return c;}catch(e){}try{let c=window.StreamBanditSupabaseConfig;if(c&&c.url&&c.key)return c;}catch(e){}return null;}
function client(){if(sb)return sb;if(!window.supabase||!window.supabase.createClient)throw Error('Supabase SDK not loaded');let c=cfg();if(!c||!c.url||!c.key)throw Error('Supabase config missing');sb=window.supabase.createClient(c.url,c.key);return sb;}
async function auth(){if(user)return user;let r=await client().auth.getUser();user=r&&r.data&&r.data.user||null;return user;}
function postIds(){return [...new Set(Array.from(document.querySelectorAll('[data-react]')).map(b=>b.getAttribute('data-react')).filter(Boolean))];}
function articleFor(id){let btn=document.querySelector('[data-react="'+CSS.escape(id)+'"]');return btn&&btn.closest('article,.card')||null;}
function actionsFor(article){return article&&(article.querySelector('.actions,.miniActions')||article);}
function postText(article){let b=article&&article.querySelector('.postBody');return clean(b&&b.textContent||document.title,220);}
function visibilityFromArticle(article){let pills=Array.from(article.querySelectorAll('.pill,.infoPill')).map(x=>String(x.textContent||'').toLowerCase().trim());return pills.includes('friends')?'friends':pills.includes('private')?'private':pills.includes('group_members')?'group_members':'public';}
function mediaKind(p){let img=String(p&&p.image_url||'').trim(),vid=String(p&&p.video_url||'').trim();return img&&vid?'mixed':img?'image':vid?'video_url':'none';}
async function loadPosts(ids){if(!ids.length)return new Map();let missing=ids.filter(id=>!cache.has(id));if(missing.length){let r=await client().from('sb_social_posts').select('id,author_id,body_text,image_url,video_url,video_provider,media_kind,visibility,target_type,status,created_at').in('id',missing);if(!r.error)(r.data||[]).forEach(p=>cache.set(p.id,p));}let out=new Map();ids.forEach(id=>{if(cache.has(id))out.set(id,cache.get(id));});return out;}
function actionBtn(label,cls,attr,id){let b=document.createElement('button');b.className='btn '+(cls||'');b.type='button';b.textContent=label;b.setAttribute(attr,id);b.dataset.sbSocialActionsRepair='true';return b;}
function ensureShare(article,id,p){if(!article||article.querySelector('[data-sb-share-target],.sbSocialShareBtn,[data-sb-share-open]'))return;let actions=actionsFor(article);let b=actionBtn('Share','cyan','data-sb-share-open',id);actions.appendChild(b);article.setAttribute('data-sb-social-share','true');article.setAttribute('data-sb-share-id',id);article.setAttribute('data-sb-share-type',p&&p.target_type?p.target_type:'post');article.setAttribute('data-sb-share-visibility',p&&p.visibility||visibilityFromArticle(article));article.setAttribute('data-sb-share-title','Stream Bandit post');article.setAttribute('data-sb-share-text',p&&p.body_text||postText(article));}
function ensureOwner(article,id,p,u){if(!article||!u||!p||String(p.author_id)!==String(u.id))return;let actions=actionsFor(article);if(!article.querySelector('[data-edit-post]'))actions.appendChild(actionBtn('Edit Post','warn','data-edit-post',id));if(!article.querySelector('[data-remove-post]'))actions.appendChild(actionBtn('Remove Post','dangerBtn','data-remove-post',id));}
function profileVisibilityFromCard(article){let txt=String(article&&article.textContent||'').toLowerCase();if(txt.includes('private'))return 'private';if(txt.includes('friends-only')||txt.includes('friends only'))return 'friends';return 'public';}
function profileNameFromCard(article){let n=article&&article.querySelector('.friendName,b,.name');return clean(n&&n.textContent||'Stream Bandit profile',90);}
function ensureFriendProfileShares(){if(file()!=='friends-social-v7-13-001-test.html')return;document.querySelectorAll('article.friendCard,article.requestCard,article.profileMini').forEach(article=>{if(article.querySelector('[data-sb-share-profile]'))return;let link=article.querySelector('a[href*="profile-social-v7-13-001-test.html"]');if(!link)return;let actions=actionsFor(article);if(!actions)return;let b=actionBtn('Share Profile','cyan','data-sb-share-profile',link.getAttribute('href')||'');actions.appendChild(b);article.setAttribute('data-sb-social-share','true');article.setAttribute('data-sb-share-type','profile');article.setAttribute('data-sb-share-visibility',profileVisibilityFromCard(article));article.setAttribute('data-sb-share-title',profileNameFromCard(article));article.setAttribute('data-sb-share-text','Stream Bandit social profile');article.setAttribute('data-sb-share-url',new URL(link.getAttribute('href')||location.href,location.href).href);});}
async function copyFallback(id){let url=new URL(location.href);url.searchParams.set('sb_share_type','post');url.searchParams.set('sb_share_id',id);try{await navigator.clipboard.writeText(url.href);toast('Share link copied.');}catch(e){prompt('Copy this share link',url.href);}}
async function sharePost(id){let p=cache.get(id)||{};let data={id,type:p.target_type||'post',visibility:p.visibility||'public',title:'Stream Bandit post',text:p.body_text||'',url:location.href};if(window.StreamBanditSocialShare&&window.StreamBanditSocialShare.open)return window.StreamBanditSocialShare.open('native',data);return copyFallback(id);}
async function shareProfile(url){let article=document.querySelector('[data-sb-share-profile="'+CSS.escape(url)+'"]')&&document.querySelector('[data-sb-share-profile="'+CSS.escape(url)+'"]').closest('article');let data={id:'profile',type:'profile',visibility:profileVisibilityFromCard(article),title:profileNameFromCard(article),text:'Stream Bandit social profile',url:new URL(url||location.href,location.href).href};if(window.StreamBanditSocialShare&&window.StreamBanditSocialShare.open)return window.StreamBanditSocialShare.open('native',data);try{await navigator.clipboard.writeText(data.url);toast('Profile link copied.');}catch(e){prompt('Copy this profile link',data.url);}}
async function editPost(id){try{let u=await auth();if(!u)return toast('Sign in first.');let p=cache.get(id);if(!p||String(p.author_id)!==String(u.id))return toast('Only the author can edit this post.');let next=prompt('Edit your post',p.body_text||'');if(next==null)return;next=String(next).trim();if(!next)return toast('Post text cannot be empty.');let res=await client().rpc('sb_social_update_own_post',{p_post_id:id,p_body_text:next,p_image_url:p.image_url||'',p_video_url:p.video_url||'',p_video_provider:p.video_provider||'',p_media_kind:p.media_kind||mediaKind(p),p_visibility:p.visibility||'public'});if(res.error)throw res.error;if(res.data!==true)return toast('No matching author-owned active post found.');p.body_text=next;cache.set(id,p);toast('Post saved.');setTimeout(()=>location.reload(),500);}catch(e){toast('Edit failed: '+(e.message||e));}}
async function removePost(id){try{let u=await auth();if(!u)return toast('Sign in first.');let p=cache.get(id);if(!p||String(p.author_id)!==String(u.id))return toast('Only the author can remove this post.');if(!confirm('Remove this post from the feed?'))return;let res=await client().rpc('sb_social_remove_own_post',{p_post_id:id});if(res.error)throw res.error;if(res.data!==true)return toast('No matching author-owned post found.');toast('Post removed.');setTimeout(()=>location.reload(),500);}catch(e){toast('Remove failed: '+(e.message||e));}}
async function mount(){if(!isSocial()||running)return;running=true;try{loadScript(SHARE_HELPER);ensureFriendProfileShares();let ids=postIds();if(ids.length){let u=await auth();let map=await loadPosts(ids);ids.forEach(id=>{let article=articleFor(id),p=map.get(id);if(article){ensureShare(article,id,p);ensureOwner(article,id,p,u);}});}if(window.StreamBanditSocialShare&&window.StreamBanditSocialShare.mount)window.StreamBanditSocialShare.mount();}catch(e){}finally{running=false;}}
function bind(){document.addEventListener('click',e=>{let edit=e.target&&e.target.closest&&e.target.closest('[data-edit-post]');if(edit){e.preventDefault();editPost(edit.getAttribute('data-edit-post'));return;}let del=e.target&&e.target.closest&&e.target.closest('[data-remove-post]');if(del){e.preventDefault();removePost(del.getAttribute('data-remove-post'));return;}let share=e.target&&e.target.closest&&e.target.closest('[data-sb-share-open]');if(share){e.preventDefault();sharePost(share.getAttribute('data-sb-share-open'));return;}let profile=e.target&&e.target.closest&&e.target.closest('[data-sb-share-profile]');if(profile){e.preventDefault();shareProfile(profile.getAttribute('data-sb-share-profile'));return;}});}
function boot(){if(!isSocial())return;bind();mount();setTimeout(mount,700);setTimeout(mount,1800);setInterval(mount,2500);try{new MutationObserver(()=>mount()).observe(document.body,{childList:true,subtree:true});}catch(e){}window.StreamBanditSocialActions={version:VERSION,mount,state:()=>({version:VERSION,page:file(),cached:cache.size,rpcEditDelete:true,friendsProfileShare:true})};document.documentElement.dataset.sbSocialActionsRepair='v7-13-004-friends-share';}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
