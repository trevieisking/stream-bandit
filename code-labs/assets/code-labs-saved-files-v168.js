/* Code Labs Saved Files Manager V168 - owner-scoped read-only list first. No delete/update yet. */
(function(){
'use strict';
var URL='https://xzxqfrvqdgkzwujbkdbk.supabase.co';
var PUB='sb_publishable_1wHhSq2xo0XBwsKXO_64HQ_xyVY9xRN';
function q(s,r){return(r||document).querySelector(s)}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}
function loadScript(){return new Promise(function(resolve,reject){if(window.supabase&&window.supabase.createClient)return resolve();var s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}
async function client(){await loadScript();if(!window.CL_SB){window.CL_SB=window.supabase.createClient(URL,PUB,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}})}return window.CL_SB}
function status(msg,kind){var el=q('#clSavedFilesStatus');if(el){el.className='badge '+(kind||'warn');el.textContent=msg}}
function help(html){var el=q('#clSavedFilesHelp');if(el)el.innerHTML=html}
function date(v){try{return new Date(v).toLocaleString()}catch(e){return String(v||'')}}
async function currentUser(){var sb=await client();var res=await sb.auth.getUser();return{sb:sb,user:res&&res.data?res.data.user:null,error:res.error}}
function renderRows(files,projects){var box=q('#clSavedFilesList');if(!box)return;projects=projects||{};if(!files.length){box.innerHTML='<div class="empty">No saved Code Labs files found for this Supabase user.</div>';return}box.innerHTML=files.map(function(f){var p=projects[f.project_id]||{};var name=f.filename||'Saved file';var project=p.site_name||p.repo||'Code Labs';var path=p.site_url||'';var chars=f.current_hash?String(f.current_hash)+' chars':'chars not set';return '<div class="item" data-cl-saved-file="'+esc(f.id)+'"><b>'+esc(name)+'</b><p>'+esc(project)+' · '+esc(f.file_type||'file')+' · '+esc(chars)+'</p><p>'+esc(path||'No saved page/path')+'</p><p>'+esc(date(f.created_at))+'</p><span class="badge warn">Read-only phase 1</span></div>'}).join('')}
async function loadFiles(){try{status('Checking Supabase','warn');help('<p><b>Phase 1:</b> read-only list only. Update and delete are intentionally disabled until the owner-scoped rows are proven correct.</p>');var cu=await currentUser();if(cu.error)throw cu.error;if(!cu.user){status('Connect Supabase','warn');help('<p><b>Connect Supabase first:</b> this page lists only files visible to the current Code Labs Supabase user. It does not use Stream Bandit app login.</p>');renderRows([],{});return{ok:false,reason:'no_user'}}status('Loading saved files','warn');var fr=await cu.sb.from('code_labs_files').select('id,project_id,filename,file_type,current_hash,created_at,metadata').order('created_at',{ascending:false}).limit(100);if(fr.error)throw fr.error;var files=fr.data||[];var ids=[];files.forEach(function(f){if(f.project_id&&ids.indexOf(f.project_id)<0)ids.push(f.project_id)});var projects={};if(ids.length){var pr=await cu.sb.from('code_labs_projects').select('id,site_name,site_url,repo,mode,created_at').in('id',ids);if(pr.error)throw pr.error;(pr.data||[]).forEach(function(p){projects[p.id]=p})}renderRows(files,projects);status('Saved files loaded','good');help('<p><b>Loaded:</b> '+files.length+' saved Code Labs file row(s). This page is read-only so no saved data can be changed from V168.</p>');return{ok:true,count:files.length}}catch(err){console.error(err);status('Load failed','bad');help('<p><b>Load failed:</b> '+esc(err.message||err)+'. No data was changed.</p>');return{ok:false,error:String(err.message||err)}}}
function render(){if((document.body&&document.body.getAttribute('data-page'))!=='saved-files')return;var main=q('.main');if(!main){setTimeout(render,120);return}if(q('#clSavedFilesV168'))return;var top=q('.topbar');main.innerHTML=(top?top.outerHTML:'')+'<section class="hero" id="clSavedFilesV168"><div><span class="pill">Code Labs support tool</span><h1>Saved Files Manager.</h1><p>Phase 1 lists Code Labs files saved in Supabase for the current owner-scoped session. It proves the correct records before update and delete controls are added.</p><div class="actions"><button class="btn primary" id="clLoadSavedFiles" type="button">Load saved files</button><a class="btn ghost" href="help.html">Help + Tools</a><a class="btn ghost" href="file-lab.html">File Lab</a></div></div><div class="heroCard"><b>Safety rules</b><ol><li>Read-only in V168.</li><li>Current Supabase user required.</li><li>Code Labs tables only.</li><li>No Stream Bandit app file deletion.</li><li>No raw SQL UI.</li></ol></div></section><section class="panel"><h2>Owner-scoped saved files</h2><p>This is the proof list for issue #270. It does not update, replace, or delete saved files yet.</p><div class="actions"><span id="clSavedFilesStatus" class="badge warn">Not checked</span><button class="btn ghost" id="clReloadSavedFiles" type="button">Refresh list</button></div><div id="clSavedFilesHelp" class="notice"><p><b>Phase 1:</b> load saved files first. Delete and replace controls will come only after this list proves the right owner rows.</p></div><div id="clSavedFilesList" class="list"><div class="empty">Saved files not loaded yet.</div></div></section><section class="notice"><p><b>Next phase after proof:</b> checkboxes, guarded replace, guarded delete, confirmation, and audit records.</p></section><div class="footerNote">Code Labs Saved Files Manager V168 · read-only proof before update/delete.</div>';q('#clLoadSavedFiles').onclick=loadFiles;q('#clReloadSavedFiles').onclick=loadFiles;window.CodeLabsSavedFilesV168={version:'V168',loadFiles:loadFiles,currentUser:currentUser,readOnly:true}}
function boot(){render();setTimeout(render,300);setTimeout(render,900)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
